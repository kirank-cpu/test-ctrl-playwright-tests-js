{
  "name": "test-capture-tests",
  "version": "1.0.0",
  "description": "Playwright E2E test framework for test-capture",
  "main": "playwright.config.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npm install && npx playwright install chromium && npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report playwright-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
